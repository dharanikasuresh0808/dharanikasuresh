1)FUNCTION PROGRAM
#include <stdio.h>
void sum() 
{
 int a,b,sum=0;
 scanf("%d%d",&a,&b);
 sum=a+b;
 printf("%d",sum);
}
int main()
{
    sum();
    sum();
    sum();
    sum();
    sum();
}

2)types of functions program
#include <stdio.h>
int add (int x,int y);
int main()
{
    int a,b,c;
    a=3;
    b=2;
    c=add(a,b);
    printf("%d",c);
}
int add(int x,int y)
{
    int z;
    z=x+y;
    return z;
}

3)types of functions
/*
no arguement no return value
no arguement with return value
with arguement no return value
with arguement with return value
*/

4)no arguement no return value

void greatnum();

int main()
{
  greatnum();   
}


void greatnum()
{
    int a,b;
    printf("enter the values:");
    scanf("%d%d",&a,&b);
    if(a>b)
    printf("a is greater");
    else
     printf("b is greater");
}

5)no arguement with return value
#include<stdio.h>

int greatnum();

int main()
{
 int a=greatnum();
 printf("%d",a);
}
int greatnum()
{
    int x,y,greatnum;
    printf("enter the values x and y:\n");
    scanf("%d%d",&x,&y);
    if(x>y)
    greatnum=x;
    else
    greatnum=y;
    
    return greatnum;
}

6)with arguement no return value

